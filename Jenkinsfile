pipeline {
    agent any
    stages {
        stage('start') {
            steps {
                echo "execute something"
                sh "ls"
                sh "sleep 3"
            }
        }
        stage('build') {
            steps {
                echo "execute something"
                sh "ls"
            }
        }
    }
}
